# =============================================================================
# hSenid Mobile Cloud Assignment Store - Environment Configuration
# =============================================================================
# Pass these as -e flags to docker run, or set on EC2 via user-data script.
# On EC2 with IAM role: AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are NOT needed.
# =============================================================================

# Application
NODE_ENV=production
PORT=8080
APP_NAME=hSenid Mobile Cloud Assignment Store

# Database (RDS endpoint in production)
# Supported dialects: postgres, mysql
DB_DIALECT=postgres
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=cloudmart
DB_USER=dbuser
DB_PASSWORD=dbpassword

# Session
SESSION_SECRET=your-super-secret-session-key-change-in-production

# AWS S3 — product image uploads
AWS_REGION=us-east-2
AWS_S3_BUCKET=aws-assignment-rsp
# Not needed on EC2 with IAM role attached:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# EFS Logs — on EC2, EFS is mounted at /logs on the host
# Container maps it via: docker run -v /logs:/app/logs
# The app writes to /app/logs inside the container
# Leave unset to use default /app/logs path
# EFS_LOGS_PATH=/app/logs

# Application URL (ALB DNS in production)
APP_URL=http://localhost:8080
